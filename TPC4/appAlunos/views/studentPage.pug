extends layout

block content
    .w3-card-4
      header.w3-container.w3-teal
          h1
            span #{aluno.name}
      .w3-container
        ul.w3-ul.w3-card-4(style="width:100%;margin: 20px auto;border-radius: 8px;")
          li 
            b Nome:
            |  #{aluno.name}
          li
            b Número: 
            | #{aluno.id}
          li
            b Git (link): 
            a(href=aluno.gitlink) #{aluno.gitlink}
        ul

        .w3-container.w3-margin-8
            ul.w3-ul
                - for(let i=1; i <= 8; i++)
                    - const key = `tpc${i}`
                    if aluno[key]
                        li
                            b TPC#{i}